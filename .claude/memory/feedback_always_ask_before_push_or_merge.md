---
name: always_ask_before_push_or_merge
description: "Always ask the user for explicit approval before performing any git push or merge operations."
metadata:
  node_type: memory
  type: feedback
---

Always ask the user for explicit permission/approval before running any command that pushes commits to a remote repository (`git push`) or merges branches (`git merge`). Never perform these actions autonomously.
